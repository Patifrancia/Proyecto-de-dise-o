Para ejecutar: cd Proyecto-de-dise-o

A) Backend: 1. cd viajar-chile-api
2. npm run dev

***
Dependencias que hay que instalar en backend:
npm i bcryptjs jsonwebtoken
npm install axios
npm install express mongoose dotenv cors bcryptjs jsonwebtoken




B) Frontend: 1. cd viajar-chile-web
2. npm install
3. npm run dev



***
Dependencias que hay que instalar en frontend:
